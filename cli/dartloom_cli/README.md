# dartloom_cli

The Dartloom CLI configures Flutter projects and selects normal Dart package
dependencies. It provides only `dartloom new`, `dartloom update`, and
`dartloom check`.

Applications own imports, object creation, dependency passing, paths, and
business behavior. The CLI never generates runtime registration or application
source code.
