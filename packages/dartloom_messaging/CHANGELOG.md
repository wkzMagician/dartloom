## 0.3.0

* Add transport-neutral encrypted blob references and storage contracts.
* Add a versioned binary attachment chunk codec for native blob transports.
* Define v2 offer, resume, chunk reference, and commit messages.
* Move ntfy and LAN implementations into dedicated adapter packages.

## 0.1.0

* Initial generic Packet, crypto, transport, deduplication and attachment APIs.
