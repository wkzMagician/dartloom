# dartloom_autostart

Desktop autostart capability contracts for Dartloom applications. It defines
`AutostartService` and a memory implementation for tests; platform adapters
can be supplied by an application without changing feature APIs.
