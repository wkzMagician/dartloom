# dartloom_autostart

Stable desktop autostart contract with an in-memory test implementation. Platform behavior is provided by adapters.

Applications normally select implementations through `dartloom cap`.
