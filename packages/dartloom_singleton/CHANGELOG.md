# Changelog

## 0.1.0

- Initial capability package. Defines the single-instance contract
  (`SingleInstanceService`), the atomic primary-claim component
  (`acquirePrimary`), the full duplicate-exit flow
  (`ensureSingleInstance`), and the two orthogonal second-instance
  dimensions (window action + argument delivery).