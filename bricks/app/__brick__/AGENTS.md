# Agent Instructions

This project is managed by Dartloom. Read `dartloom.yaml`, keep business code in `lib/features`, and run `dart format .`, `flutter analyze`, and `flutter test` before finishing.
