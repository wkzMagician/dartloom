# Dartloom Flutter App Template

`dartloom new` uses Flutter's supported project generator, then applies the Dartloom structure and managed files contained in this template specification. This keeps generated platform projects aligned with the installed Flutter SDK.
